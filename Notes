def sanitize_text(value):
            if isinstance(value, str):
                return value.replace("\n", " ").strip()  # Replace newline with space and strip whitespace
            return value

        # Sanitize all data in the input list
        sanitized_data = [
            {key: sanitize_text(value) for key, value in record.items()}
            for record in data
        ]

        